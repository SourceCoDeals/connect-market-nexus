/**
 * DashboardErrorBanner — re-export of the shared banner in
 * `@/components/common/DashboardErrorBanner` so existing imports in the
 * remarketing dashboard keep working. Prefer importing from the shared
 * location for new call sites.
 */
export { DashboardErrorBanner } from '@/components/common/DashboardErrorBanner';
