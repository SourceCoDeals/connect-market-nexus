/**
 * ConnectionRequestActions.tsx
 *
 * Re-export from the decomposed connection-request-actions directory.
 * Kept for backwards compatibility with existing import paths.
 */
export { ConnectionRequestActions } from './connection-request-actions';
export type { ConnectionRequestActionsProps } from './connection-request-actions';
