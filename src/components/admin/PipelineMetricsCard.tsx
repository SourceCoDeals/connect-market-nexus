import { AdminConnectionRequest } from "@/types/admin";

interface PipelineMetricsCardProps {
  requests: AdminConnectionRequest[];
}

export function PipelineMetricsCard({ requests: _requests }: PipelineMetricsCardProps) {
  return null;
}
