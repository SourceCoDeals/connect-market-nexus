export const PIPELINE_FEATURES = {
  customViews: true,
  stageLibrary: true,
  stageCustomization: true, // add/remove stages
  stageReordering: true,
} as const;

export const DISABLED_FEATURE_MESSAGE = 'Admin disabled these features. To enable, contact Adam.';
