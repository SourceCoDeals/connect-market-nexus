# =============================================================================
# Environment Variables for connect-market-nexus (SourceCo Platform)
# =============================================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control.
# Last updated: 2026-02-26 (CTO Audit)
# =============================================================================

# ---------------------------------------------------------------------------
# Client-Side (Vite — exposed to browser via import.meta.env)
# ---------------------------------------------------------------------------

# Supabase project ID (found in project settings)
VITE_SUPABASE_PROJECT_ID=""

# Supabase anon/public API key (safe to expose in client-side code)
VITE_SUPABASE_PUBLISHABLE_KEY=""

# Full Supabase project URL (e.g., https://<project-id>.supabase.co)
VITE_SUPABASE_URL=""

# Mapbox token for geographic visualisations (optional)
VITE_MAPBOX_ACCESS_TOKEN=""

# ---------------------------------------------------------------------------
# Server-Side — Supabase Edge Functions (Deno.env)
# These are set in the Supabase dashboard under Edge Function secrets.
# ---------------------------------------------------------------------------

# ── Core Infrastructure ──────────────────────────────────────────────────────

# Supabase URL (same as VITE_SUPABASE_URL but for server-side)
SUPABASE_URL=""

# Supabase anonymous key
SUPABASE_ANON_KEY=""

# Supabase service role key (NEVER expose client-side)
SUPABASE_SERVICE_ROLE_KEY=""

# ── AI / LLM Services ────────────────────────────────────────────────────────

# Anthropic API key for Claude (AI Command Center)
ANTHROPIC_API_KEY=""

# Google Gemini API key (deal/buyer enrichment AI extraction)
GEMINI_API_KEY=""

# OpenAI API key (optional, fallback)
OPENAI_API_KEY=""

# ── Contact Enrichment ────────────────────────────────────────────────────────

# Apify API key for LinkedIn scraping and Google search
APIFY_API_KEY=""

# Prospeo API key for email/phone enrichment
PROSPEO_API_KEY=""

# Firecrawl API key for website scraping
FIRECRAWL_API_KEY=""

# ── Integrations ──────────────────────────────────────────────────────────────

# Fireflies API key for call transcript sync
FIREFLIES_API_KEY=""

# DocuSeal API key for e-signatures (NDAs, fee agreements)
DOCUSEAL_API_KEY=""

# DocuSeal template IDs
DOCUSEAL_NDA_TEMPLATE_ID=""
DOCUSEAL_FEE_TEMPLATE_ID=""

# Smartlead API key for email campaigns
SMARTLEAD_API_KEY=""

# HeyReach API key for LinkedIn outreach
HEYREACH_API_KEY=""

# ── Webhook Secrets ───────────────────────────────────────────────────────────

# DocuSeal webhook verification
DOCUSEAL_WEBHOOK_SECRET=""
DOCUSEAL_WEBHOOK_SECRET_HEADER=""

# Smartlead webhook verification
SMARTLEAD_WEBHOOK_SECRET=""

# PhoneBurner webhook verification
PHONEBURNER_WEBHOOK_SECRET=""

# HeyReach webhook verification
HEYREACH_WEBHOOK_SECRET=""

# Cron job secret (for scheduled functions)
CRON_SECRET=""

# ── Email Delivery ────────────────────────────────────────────────────────────

# Brevo (Sendinblue) API key for transactional email
BREVO_API_KEY=""

# Resend API key (alternative email provider)
RESEND_API_KEY=""

# Email sender configuration
SENDER_EMAIL=""
SENDER_NAME=""
NOREPLY_EMAIL=""
ADMIN_EMAIL=""
ADMIN_NOTIFICATION_EMAIL=""
DEALS_EMAIL=""

# Owner inquiry email configuration
OWNER_INQUIRY_RECIPIENT_EMAIL=""
OWNER_INQUIRY_RECIPIENT_NAME=""
OWNER_INQUIRY_SENDER_EMAIL=""

# ── CapTarget (Google Sheets) ─────────────────────────────────────────────────

# Google service account credentials (JSON key)
GOOGLE_SERVICE_ACCOUNT_KEY=""

# CapTarget Google Sheet ID and tab names
CAPTARGET_SHEET_ID=""
CAPTARGET_ACTIVE_TAB_NAME=""
CAPTARGET_INACTIVE_TAB_NAME=""

# ── Environment & Network ────────────────────────────────────────────────────

# Environment: development | staging | production
ENVIRONMENT=""

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=""

# Base URL for API and site
BASE_URL=""
SITE_URL=""

# Internal email domains (comma-separated, for filtering)
INTERNAL_EMAIL_DOMAINS=""
